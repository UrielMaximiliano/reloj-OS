# Experiment: steps

Command:

```powershell
.\.venv\Scripts\python.exe scripts\monitor.py --device FRG --label steps --duration 90 --all
```

Record the displayed step count before and after walking approximately 20-50 steps. No exact count is required; the variation is the control variable.

## Run record

- Date/time UTC:
- Firmware:
- Before steps:
- After steps:
- Approximate steps added:
- Capture:
- UUIDs involved:
- Packets:
- Differences versus idle:
- Interpretation:
- Confidence: `UNKNOWN`
