# Experiment: brightness

Command:

```powershell
.\.venv\Scripts\python.exe scripts\monitor.py --device FRG --label brightness --duration 60 --all
```

Change brightness on the watch through at least three levels. If no traffic appears, record `NO BLE TRAFFIC OBSERVED`; that is a valid result.

## Run record

- Date/time UTC:
- Firmware:
- Initial brightness:
- Levels selected:
- Capture:
- UUIDs involved:
- Packets:
- Difference versus idle:
- Interpretation:
- Confidence: `UNKNOWN`
